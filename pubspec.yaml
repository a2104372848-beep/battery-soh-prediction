name: battery_soh_prediction
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  provider: ^5.0.0
  charts_flutter: ^0.12.0
  flutter_material: ^0.1.0
